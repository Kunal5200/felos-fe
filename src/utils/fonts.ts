import { Poppins } from "next/font/google";

export const poppins = Poppins({
  weight: "400",
  subsets: ["latin"],
});

export const boldPoppins = Poppins({
  weight: "800",
  subsets: ["latin"],
});
